# AI_AGENT_WIZARD - TESTING
# LAST_UPDATED: 2026-02-16

health_checks:
  - name: Sistema activo
    cmd: "echo OK"

test_commands:
  unit: "npm test"

quick_validation:
  python: "python -m py_compile {archivo}"
  javascript: "node --check {archivo}"
  json: "python -m json.tool {archivo} > /dev/null"
